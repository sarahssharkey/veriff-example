import { MESSAGES } from './messages';
interface VeriffFrameOptions {
    url: string;
    lang?: string;
    onEvent?: (msg: MESSAGES) => void;
    onReload?: () => void;
}
interface VeriffFrame {
    close: () => void;
}
declare function createVeriffFrame({ url, lang, onEvent, onReload, }: VeriffFrameOptions): VeriffFrame;
export { createVeriffFrame, MESSAGES, };
